#!/bin/sh
PATH="/sbin:/usr/sbin:/bin:/usr/bin:$PATH"

# this runs spamd under supervise

# source sysconfig settings
if [ -f /etc/sysconfig/spamassassin ]; then
	. /etc/sysconfig/spamassassin
else
	OPTIONS="-c -a -m5 -H"
fi

exec /usr/bin/spamd $OPTIONS
