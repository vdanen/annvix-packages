--- initscripts-7.06/rc.d/rc.local.mdk	2003-11-23 08:16:50.000000000 -0700
+++ initscripts-7.06/rc.d/rc.local	2003-11-23 08:17:38.000000000 -0700
@@ -10,8 +10,8 @@
 # Source functions
 . /etc/init.d/functions
 
-if [ -f /etc/mandrake-release -a $SECURE_LEVEL -lt 4 ]; then
-    R=$(cat /etc/mandrake-release)
+if [ -f /etc/opensls-release -a $SECURE_LEVEL -lt 4 ]; then
+    R=$(cat /etc/opensls-release)
 
     arch=$(uname -m)
     a="a"
@@ -49,11 +49,12 @@
 	echo "$R" >> /etc/issue.net
 	echo "Kernel $(uname -r) on $a $SMP$(uname -m)" >> /etc/issue.net
     else	
-	echo "Welcome to Mandrake Linux" > /etc/issue.net
-	echo "-------------------------" >> /etc/issue.net
+	echo "Welcome to OpenSLS" > /etc/issue.net
+	echo "------------------" >> /etc/issue.net
     fi
 elif [ $SECURE_LEVEL -ge 4 ]; then
     rm -f /etc/issue /etc/issue.net
 fi
 
 touch /var/lock/subsys/local
+u
\ No newline at end of file
--- initscripts-7.06/rc.d/rc.sysinit.mdk	2003-11-23 08:18:42.000000000 -0700
+++ initscripts-7.06/rc.d/rc.sysinit	2003-11-23 08:19:12.000000000 -0700
@@ -99,9 +99,9 @@
 # so real escap chars were written
 PRODUCT=`sed "s/.*release \([0-9.]*\).*/\1/g" /etc/mandrake-release 2> /dev/null`
 if [ "$BOOTUP" != "serial" ]; then
- gprintf "\t\t\tWelcome to %sMandrake%s Linux %s" `echo -en "\\033[1;36m"` `echo -en "\\033[0;39m"` "$PRODUCT"
+ gprintf "\t\t\tWelcome to %sOpenSLS%s %s" `echo -en "\\033[1;36m"` `echo -en "\\033[0;39m"` "$PRODUCT"
 else
- gprintf "\t\t\tWelcome to %sMandrake%s Linux %s" "" "" "$PRODUCT"
+ gprintf "\t\t\tWelcome to %sOpenSLS%s %s" "" "" "$PRODUCT"
 fi
 echo -en "\r"
 echo
--- initscripts-7.06/sysctl.conf.mdk	2003-11-23 08:19:54.000000000 -0700
+++ initscripts-7.06/sysctl.conf	2003-11-23 08:19:54.000000000 -0700
@@ -1,4 +1,4 @@
-# Kernel sysctl configuration file for Mandrake Linux
+# Kernel sysctl configuration file for OpenSLS
 #
 # For binary values, 0 is disabled, 1 is enabled.  See sysctl(8) and
 # sysctl.conf(5) for more details.
