--- linux-2.4.22/drivers/usb/serial/usbserial.c.np	2003-08-25 13:44:42.000000000 +0200
+++ linux-2.4.22/drivers/usb/serial/usbserial.c	2003-09-05 23:29:38.000000000 +0200
@@ -576,10 +576,11 @@ static void serial_close(struct tty_stru
 	dbg("%s - port %d", __FUNCTION__, port->number);
 
 	/* if disconnect beat us to the punch here, there's nothing to do */
-	if (tty->driver_data) {
+	if (tty && tty->driver_data) {
+		tty->driver_data = NULL;
 		__serial_close(port, filp);
 	}
-
+	port->tty = NULL;
 	up (&port->sem);
 }
 
