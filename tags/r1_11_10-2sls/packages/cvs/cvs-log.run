#!/bin/sh

# this writes logs as user nobody into /var/log/supervise/cvspserver; make 
# sure this directory exists and is writable by nobody!

exec /usr/bin/setuidgid nobody /usr/bin/multilog t /var/log/supervise/cvspserver