service ftp
{
	disable = no
	flags           = REUSE
	socket_type     = stream
	wait            = no
	user            = root
	server          = /usr/sbin/ftpd
	server_args     = -l
	log_on_failure  += USERID
}

