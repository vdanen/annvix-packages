--- subversion-1.3.0-rc4/svn-config.in--	2005-12-06 11:29:37.000000000 +0100
+++ subversion-1.3.0-rc4/svn-config.in	2005-12-06 11:30:58.000000000 +0100
@@ -66,7 +66,13 @@
 thisdir="`dirname $0`"
 thisdir="`cd $thisdir && pwd`"
 if test -d $bindir; then
-  tmpbindir="`cd $bindir && pwd`"
+  mplatform=`which multiarch-platform`
+  if test -n "$mplatform" -a -x "$mplatform"; then
+	tmpbindir="$bindir/`$mplatform`"
+  else
+	tmpbindir="$bindir"
+  fi
+  tmpbindir="`cd $tmpbindir && pwd`"
 else
   tmpbindir=""
 fi
